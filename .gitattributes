# Normalize line endings to LF on commit so linters and diffs stay consistent
# across machines (the repo previously mixed CRLF/LF).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
